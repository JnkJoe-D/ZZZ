# 现代游戏客户端核心架构设计规范与工程指南
# (Modern Game Client Architecture & Engineering Specification)

> [!IMPORTANT]
> **核心哲学：设计原则指引决策方向，设计模式解决具体问题，工程约束控制实现成本。**
> 
> 本规范是一份**通用、纯净、独立于具体游戏项目**的标准工业级客户端架构设计指南。旨在为中大型游戏（尤其是实时动作、RPG 及复杂交互游戏）提供清晰的分层解耦、状态管理、时钟拓扑、生命周期控制及反模式防御标准，建立高内聚、低耦合、易测试、可扩展且具备卓越性能的软件系统骨干。

---

# 目录 (Table of Contents)

1. [架构哲学与核心原则 (Architecture Philosophy)](#1-架构哲学与核心原则-architecture-philosophy)
2. [宏观系统分层架构 (Macro System Layering)](#2-宏观系统分层架构-macro-system-layering)
3. [领域内部四级垂直职责契约 (Internal Domain Hierarchy)](#3-领域内部四级垂直职责契约-internal-domain-hierarchy)
4. [四维正交解耦模型 (The Four-Way Orthogonal Separation)](#4-四维正交解耦模型-the-four-way-orthogonal-separation)
5. [控制反转与表现装配机制 (Presentation Assembly & IoC)](#5-控制反转与表现装配机制-presentation-assembly--ioc)
6. [分层时钟树与受控 Tick 调度拓扑 (Clock Tree & Tick Pipeline)](#6-分层时钟树与受控-tick-调度拓扑-clock-tree--tick-pipeline)
7. [对象与资源生命周期闭环 (Lifecycle & Ownership)](#7-对象与资源生命周期闭环-lifecycle--ownership)
8. [反模式禁令与交付自查清单 (Anti-Patterns & Checklist)](#8-反模式禁令与交付自查清单-anti-patterns--checklist)

---

# 1. 架构哲学与核心原则 (Architecture Philosophy)

### 1.1 架构的核心目标
软件架构不是为了追求形式上的对称与繁复，而是为了在游戏全生命周期中实现以下目标：
1. **控制认知负载 (Manage Cognitive Load)**：让开发者在修改任意单一系统时，只需关注局部上下文，无需在脑中加载整个游戏的代码；
2. **隔离易变边界 (Isolate Volatility)**：将易变的业务逻辑（策划数值、手感微调）与相对稳定的基础设施（网络、渲染管线、资源管理）严格隔离开；
3. **保证高吞吐量与持续交付 (Sustain Velocity)**：随着项目规模扩大，研发团队的人效不应呈指数级下降，代码修改不会引发“改 A 坏 B”的连带雪崩；
4. **保障严苛性能预算 (Protect Performance Budget)**：架构必须自然引导开发者编写缓存友好、GC 分配极低、帧时间稳定可控的高性能代码。

### 1.2 工程优先级法则
当不同质量属性发生冲突时，默认遵循以下绝对优先级：

$$\mathbf{正确性} \gt \mathbf{可维护性} \gt \mathbf{可理解性} \gt \mathbf{可扩展性} \gt \mathbf{性能优化} \gt \mathbf{形式优美}$$

> [!NOTE]
> **关于前瞻性优化的警示**：脱离 Profiler 数据与实际瓶颈前瞻性地引入非必要的间接层（过度使用通用反射、无意义的抽象中介、复杂的元编程），本身就是极高昂的技术负债。**推迟非必要的抽象，直到真实重复的变动模式稳定显现。**

---

# 2. 宏观系统分层架构 (Macro System Layering)

现代游戏客户端必须划分为清晰的洋葱式或单向流动分层。物理目录、程序集定义（Assembly Definitions）与命名空间必须严格与分层拓扑单向对齐。

```text
┌─────────────────────────────────────────────────────────────────────────────┐
│ 5. 应用组合根与主流程驱动 (Application / Bootstrap)                          │
│    • 职责：唯一主入口引导、状态机推进（Init -> Login -> Hall -> Battle）   │
└─────────────────────────────────────────────────────────────────────────────┘
                                       │
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ 4. 视听呈现层 (Presentation / View Layer)                                   │
│    • 职责：UI 界面、骨骼动画驱动、粒子特效、声音音效、镜头相机追踪          │
│    • 契约：只读消费 Domain 数据与事件；绝对禁止反向侵入修改核心玩法状态     │
└─────────────────────────────────────────────────────────────────────────────┘
                                       │
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ 3. 玩法领域业务层 (Gameplay / Domain Layer)                                 │
│    • 职责：实体聚合根、状态机、战斗结算管线、技能判定、寻路与决策 AI        │
│    • 契约：纯领域业务规则；通过接口契约与事件总线间接驱动表现层，对 View 零依赖│
└─────────────────────────────────────────────────────────────────────────────┘
                                       │
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ 2. 核心通用框架层 (Core Framework Layer)                                    │
│    • 职责：对象池基建、类型安全事件中心、FSM 内核、层次时钟基类、通用数学库 │
│    • 契约：通用游戏开发工具集，绝对禁止依赖任何具体玩法业务与表现层逻辑     │
└─────────────────────────────────────────────────────────────────────────────┘
                                       │
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ 1. 基础设施与平台引擎适配 (Infrastructure / Engine Adaptor)                 │
│    • 职责：引擎生命周期桥接、网络 Socket 通信、文件 I/O、持久化、第三方 SDK │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 单向依赖红线 (The Golden Dependency Rules)
1. **严格向下依赖**：上层系统可调用下层系统的公开契约，**严禁逆向跨层依赖**（例如：`Framework` 绝对不能引用 `Gameplay`，`Gameplay` 绝对不能引用 `Presentation` 具体类）；
2. **同层水平解耦**：同层不同子模块之间优先通过**领域事件 (Domain Events)** 或 **上下文参数 (Context Passing)** 解耦，禁止紧密的直接双向引用；
3. **零循环依赖**：程序集级别禁止任何循环依赖，运行时程序集绝对不能引用编辑器开发程序集。

---

# 3. 领域内部四级垂直职责契约 (Internal Domain Hierarchy)

在玩法领域层（`Gameplay / Domain`）内部，避免将所有逻辑扁平化散落在引擎脚本中，统一收敛为**四级垂直职责契约**：

```text
Manager / Orchestrator (大盘调度与系统编排服务)
       │  [生命周期所有权 / 单向遍历驱动]
       ▼
Entity / Aggregate Root (聚合根与领域实体宿主)
       │  [能力挂载 / 局部时钟下发]
       ▼
Component / Engine Adaptor (引擎适配组件 - 宿主特定扩展)
       │  [引擎生命周期与物理渲染事件监听]
       ▼
Module / Controller / Pipe (纯原生领域对象 - POCO / Plain Objects)
```

### 3.1 大盘调度与系统编排服务 (Manager / Orchestrator)
- **定位**：局内或全局单例/静态服务，负责宏观规则统筹、实体生成工厂管理、生命周期所有权闭环与多实体间的协调；
- **核心契约**：
  1. **所有权闭环**：负责实例化的管理器必须负责其完整的销毁与回收，下层对象不得越权反向调用回收接口；
  2. **避免全局上下文缓存**：禁止管理器在内部静态字典中强引用实体内部的私有运行上下文，杜绝实体销毁后的隐蔽内存泄漏；
  3. **集中驱动**：管理器负责统一遍历名下活跃实体，调用其逻辑更新，而非让数以千计的实体在引擎底层散乱并发注册。

### 3.2 聚合根与领域实体 (Entity / Aggregate Root)
- **定位**：游戏领域内的核心聚合根，代表战斗世界中的玩家、怪物、可交互机关或载具；
- **核心契约**：
  1. **资产与身份持有**：持有唯一的实例 ID、空间位置与配置资产；
  2. **组件聚合容器**：作为各专职能力的容器（宿主），持有并协调运动、战斗、状态与表现接口；
  3. **纯壳化设计**：实体主体应尽量保持轻量，只作为引擎事件桥接与各领域模块的装配中心，业务计算委托给专用模块。

### 3.3 引擎适配组件 (Component / Engine Adaptor)
- **定位**：挂载于引擎实体对象上的适配组件（如 Unity 的 `MonoBehaviour`、Unreal 的 `ActorComponent`）；
- **核心契约**：
  1. **严格限定职责范围**：仅负责响应引擎专属生命周期（Transform 同步、动画机根运动提取、物理碰撞触发、视锥裁剪感知）；
  2. **不得擅自开辟独立调度**：组件不得自作主张开启不受控的独立线程、协程或全局轮询来修改实体业务状态。

### 3.4 纯原生领域模块与无状态管线 (Module / Controller / Pipe - Plain Objects)
- **定位**：无引擎特定依赖的纯面向对象结构（POCO），承载纯粹的领域状态机、伤害结算公式、AI 决策树与数值计算；
- **核心契约**：
  1. **脱机可测试**：脱离游戏引擎编辑器与渲染场景依然能在控制台环境下以微秒级速度执行单元测试；
  2. **管线确定性**：管线处理器优先设计为无状态、输入输出确定的纯函数或上下文穿透模式（Context Pipeline）。

---

# 4. 四维正交解耦模型 (The Four-Way Orthogonal Separation)

现代高质量游戏系统的抗熵增能力，主要建立在以下四个维度的**绝对正交隔离**之上：

```text
┌─────────────────────────────────────────────────────────────────────────────┐
│ 1. 静态配置数据 (Static Config Data)                                         │
│    • 表现形式：只读配置表、数据资产定义、技能 Timeline 静态数据               │
│    • 核心规则：运行期绝对只读 (Immutable)、跨实例共享、无任何可变运行时状态  │
└─────────────────────────────────────────────────────────────────────────────┘
                                       │ 实例化 / 驱动
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ 2. 运行时状态容器 (Runtime State Data)                                       │
│    • 表现形式：实体私有状态模型 (如：当前生命值、冷却倒计时、无敌窗口标记)   │
│    • 核心规则：每实例独立隔离、易变、可重置、对外提供只读查询契约             │
└─────────────────────────────────────────────────────────────────────────────┘
                                       │ 状态变更被动触发 / 决策输入
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ 3. 业务规则与逻辑引擎 (Business Logic & Logic Engine)                        │
│    • 表现形式：状态机流转、伤害结算管线、技能判定器、AI 行为树               │
│    • 核心规则：权威逻辑决策中枢；根据输入与状态运算新状态，派发领域事件      │
└─────────────────────────────────────────────────────────────────────────────┘
                                       │ 消费数据 / 驱动渲染
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ 4. 视听表现呈现层 (Presentation Layer)                                       │
│    • 表现形式：骨骼动画驱动、粒子特效、模型材质、受击音效、UI 动效          │
│    • 核心规则：纯单向消费者；绝对禁止反向私自篡改核心玩法状态！               │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 维度正交红线明细

#### 1. 静态配置 (Static Config Data)
- **严禁数据污染**：配置资产在运行期属于跨实体共享内存，绝对禁止向配置资产字段中写入任何动态数据（如“当前充能数”、“最近使用时间”）；
- **配置与执行分离**：配置资产只描述“是什么”（数值定义、时间轴关键帧），不包含“怎么执行”的具体业务流（执行器应在纯领域服务中）。

#### 2. 运行时状态 (Runtime State Data)
- **单一真理源 (Single Source of Truth)**：实体的所有核心判定必须且仅能向其运行时状态容器查询，严禁向表现层询问业务状态（例如：严禁向动画机查询是否处于受击硬直，必须直读逻辑状态机或数据容器）；
- **只读封装契约**：状态容器对外暴露只读属性或只读集合接口，修改权限仅向对应的业务控制器开放。

#### 3. 业务规则 (Business Logic Engine)
- **逻辑确定性**：逻辑判定的成功与否，绝不依赖视听资源加载是否完成或特定特效是否播放成功；
- **事件通知机制**：业务产生关键状态转移（如角色死亡、护盾击破、触发完美闪避）时，向事件总线广播领域事件，由表现层自发响应，业务层不持有 View 引用。

#### 4. 视听表现 (Presentation Layer)
- **最高防御红线**：
  > [!CAUTION]
  > **表现层只能单向消费状态与事件，绝对禁止反向私自篡改核心战斗状态或破坏玩法判定流程！**
  > 
  > ❌ **反例**：等待特效粒子播放完毕，由粒子回调脚本去清除怪物的“硬直”状态（必须由逻辑时钟倒计时结束直接触发）；
  > ❌ **反例**：在动画帧事件中直接为玩家扣除法力值或赋予伤害加成。

---

# 5. 控制反转与表现装配机制 (Presentation Assembly & IoC)

在严格单向依赖的架构下，玩法层（`Gameplay`）绝对不能引用具体表现层（`Presentation`）的类。然而实体在实际运行中又必须具备显隐控制、碰撞层切换、模型偏移与相机追踪等表现能力。

为了解决这一矛盾，采用**依赖倒置原则（Dependency Inversion Principle, DIP）与表现装配器（Presentation Binder / Assembly Factory）**：

```text
   【1_GamePlay 纯玩法层】                          【2_Presentation 表现层】
┌───────────────────────────┐                    ┌───────────────────────────┐
│     IEntityPresentation   │ ◄───────────────── │  EntityPresentationComp   │
│       (表现抽象契约)      │   实现表现接口契约   │    (具体表现实现组件)     │
└─────────────┬─────────────┘                    └─────────────┬─────────────┘
              ▲                                                │
              │ 依赖抽象                                       │ 运行时自动注册
              │                                                ▼
┌─────────────┴─────────────┐                    ┌───────────────────────────┐
│ PresentationAssemblyBinder│ ◄───────────────── │ Auto-Registration Hook   │
│   (装配工厂注册表与中介)   │   注册具体装配工厂   │ (RuntimeInit / Bootstrap) │
└─────────────┬─────────────┘                    └───────────────────────────┘
              ▲
              │ 缺失时动态请求装配
┌─────────────┴─────────────┐
│        RoleEntity         │
│     (聚合根业务宿主)      │
└───────────────────────────┘
```

### 标准装配工作流
1. **契约下沉**：在纯玩法层定义轻量表现契约接口（如 `IEntityPresentation`、`IVisualOffsetPresenter`、`ICameraController`）；
2. **注册中介**：在纯玩法层设立静态装配中介（`PresentationAssemblyRegistry`），提供 `Register(Func<GameObject, Entity, IPresentation>)` 与 `Bind(GameObject, Entity)`；
3. **表现层自注册**：表现层组件在其模块引导期（如程序集加载或场景启动前钩子）自动将具体的组件构造委托注册给装配中介；
4. **渐进式兜底绑定**：
   - 实体初始化时，优先通过引擎原生检索（`GetComponent<IEntityPresentation>()`）命中已预制在资产上的表现组件；
   - 若资产未配置该组件，实体通过装配中介动态请求装配，表现层装配工厂即时补全挂载；
5. **架构收益**：
   - 玩法层 100% 保持纯净，对表现层组件类**零反向依赖**；
   - 即使美术制作的预制体资产遗漏了表现组件，运行时自动装配与生命周期绑定机制也能提供**自愈兜底**，防止空指针与功能断裂。

---

# 6. 分层时钟树与受控 Tick 调度拓扑 (Clock Tree & Tick Pipeline)

在高速动作、即时战略及包含“子弹时间”、“卡肉顿帧”、“局内暂停”的游戏中，平铺依赖全局引擎时间（如 `Time.deltaTime`）会导致严重的手感崩坏、穿帮与状态互斥死锁。

系统必须构建**层次化时钟树（Hierarchical Clock Tree）**与**四象限时间拓扑**。

### 6.1 四象限时间拓扑规范

```text
                  【工业级游戏客户端四象限时间拓扑】

          [象限 1：输入与手感域]               [象限 4：UI与交互系统域]
          - 驱动源: 真实物理世界时间           - 驱动源: 独立 UI 时钟 (UIClock)
          - 特征: 100% 免疫任何减速与定格     - 特征: 局内战斗暂停时依然流畅响应
          - 范畴: 指令缓冲、预输入、点按/长按  - 范畴: 弹窗、伤害飘字、菜单动效、网络心跳
          ──────────────────────────────────────────────────────────
          [象限 2：纯玩法与规则域]             [象限 3：运动学与视听表现域]
          - 驱动源: 实体专属局部时钟 (Clock)   - 驱动源: 混合自适应时钟
          - 特征: 严格受实体当前 EffectiveScale- 特征: 动作与位移受实体局部时钟驱动；
          - 范畴: 动作时钟、技能CD、硬直倒计时         全屏后处理与环境视听受真实时间驱动
                  Buff结算、AI思考判定         - 范畴: 根运动位移、拖尾特效、震屏、动态音调
```

1. **输入手感域（象限 1）必须绝对脱敏**：
   玩家的预输入缓冲与指令超时计算，必须基于真实世界时间。在 0.1x 慢动作下，原本 0.2 秒的输入窗口严禁被同比例膨胀为 2 秒，彻底杜绝输入黏滞与幽灵出招；
2. **纯玩法域（象限 2）局部自治**：
   实体的动作播放、技能冷却、Buff 计时及受击硬直倒计时，严格由实体自身的局部时钟流速计算推进；
3. **运动学与表现域（象限 3）流速对齐**：
   涉及角色的动画根运动（Root Motion）提取与角色控制器贴地物理位移，计算位移时必须乘上实体当前局部流速，彻底消除慢动作下的“太空步滑行”穿帮；
4. **UI 交互域（象限 4）暂停隔离**：
   战斗主时钟暂停（流速归零）时，UI 时钟保持独立，保证全屏暂停时设置菜单与界面粒子平滑过渡。

---

### 6.2 复合模式时钟树拓扑与级联定律

```text
RootClock (全局主时钟)
  ├── UIClock (独立 UI 时钟节点，局内暂停时保持 1.0x)
  └── GameplayClock (核心战斗时钟，局内暂停时 LocalScale = 0.0x)
        ├── PlayerGroupClock (玩家小队时钟通道)
        │     └── EntityClock_Player (玩家实体私有时钟)
        ├── MonsterGroupClock (敌方大盘时钟通道，进入极限闪避子弹时间时 LocalScale = 0.1x)
        │     ├── EntityClock_MonsterA (怪物 A 顿帧定格时 LocalScale = 0.0x)
        │     └── EntityClock_MonsterB (怪物 B 巡逻中 LocalScale = 1.0x)
        └── EnvironmentClock (场景环境与机关通道)
```

- **级联缩放定理 (The Cascade Scale Law)**：
  任意时钟节点的有效时间流速（$\text{EffectiveScale}$），等于其自身局部流速与所有父级节点有效流速的级联累乘：
  $$\text{EffectiveScale}_{\text{node}} = \text{LocalScale}_{\text{node}} \times \text{EffectiveScale}_{\text{parent}}$$

- **顿帧与子弹时间的数学解耦（零条件分支自愈）**：
  - **触发子弹时间**：仅需将 `MonsterGroupClock.LocalScale = 0.1f`，整棵怪物子树所有实体的实际流速自动级联降为 `0.1x`；新生成的怪物挂接到该通道下天然自动继承 `0.1x`；
  - **单体受击顿帧（卡肉）**：仅需将该受击怪物的私有时钟 `monster.Clock.LocalScale = 0.0f`：
    $$\text{EffectiveScale} = 1.0(\text{Global}) \times 1.0(\text{Gameplay}) \times 0.1(\text{MonsterGroup}) \times 0.0(\text{Local}) = \mathbf{0.0x} \quad (\text{瞬间定格})$$
  - **顿帧 0.06s 结束**：将 `monster.Clock.LocalScale = 1.0f`：
    $$\text{EffectiveScale} = 1.0 \times 1.0 \times 0.1 \times 1.0 = \mathbf{0.1x} \quad (\text{自动平滑恢复为慢动作})$$
  - **架构收益**：完全消除了在代码各处编写 `if (isInBulletTime) ...` 等海量脆弱的状态判断，纯数学级联保证了状态天然互不冲突、自愈闭合。

---

### 6.3 自顶向下单向 Tick 推进链条

坚决杜绝“每个实体、每个组件在底层引擎原生的并发更新中各算各的时间”，收敛为确定性的单向 Tick 驱动链：

```text
[TimeManager / 主循环中枢]
        │ 1. 推进时钟树根节点 RootClock.Advance(engineDeltaTime)
        │ 2. 广播受控逻辑步长 OnGameplayLogicTick(logicDeltaTime)
        ▼
[Manager / 业务编排服务]
        │ 3. 集中调度，遍历持有的活跃实体列表
        ▼
[Entity.OnLogicTick(logicDeltaTime)]
        │ 4. 实体推进私有时钟 Clock.Advance(logicDeltaTime)
        │ 5. 计算实体局部缩放步长 scaledDt = logicDeltaTime * Clock.EffectiveScale
        ├──► ActionPlayer.Tick(scaledDt)     (动作时间轴采样)
        ├──► FSMSystem.Update(scaledDt)       (状态机流转)
        ├──► AIController.Tick(scaledDt)      (AI 行为决策)
        └──► StatusModule.OnLogicTick(scaledDt) (Buff 与数值倒计时)
```

---

# 7. 对象与资源生命周期闭环 (Lifecycle & Ownership)

### 7.1 生命周期五问黄金准则 (The Five Questions of Lifecycle)
在设计任何类、动态实体、缓存容器或资源加载句柄时，必须给出清晰的回答：
```text
1. 谁创建它？ (Who creates it?)   → 明确唯一的工厂或创建所有者
2. 谁持有它？ (Who owns it?)      → 明确强引用容器，严禁模糊的多头管理
3. 谁使用它？ (Who uses it?)      → 通过明确接口暴露使用入口或只读查询
4. 谁释放它？ (Who releases it?)  → 谁负责创建，就由谁负责回收或销毁
5. 何时释放？ (When is it released?) → 明确在死亡延时、战斗结算或场景切换时释放
```

### 7.2 对象池生命周期协议 (Object Pool Lifecycle Protocol)
进入对象池管理的实体必须实现标准的生命周期契约：
- **`OnPoolSpawn()`**：重置所有运行时状态容器，重新装配私有时钟树，开启可见性与碰撞；
- **`OnPoolDespawn()`**：停止所有异步协程与动作播放，清除指令缓冲与速度，关闭碰撞，脱离时钟树，断开临时外部引用。

### 7.3 异步操作防御与取消拓扑 (Async Task Cancellation)
所有异步任务（`async/await`、协程、Tween 动效）必须具备以下防御机制：
1. **宿主销毁感知**：异步任务必须绑定宿主对象的生命周期取消令牌（如 `CancellationToken`）；当宿主销毁时，异步任务必须立即安全终止；
2. **场景卸载熔断**：跨场景异步加载在离开当前场景时必须无条件中止，防止已销毁场景的回调写入导致空引用异常；
3. **防重复并发重入**：对不可重入的操作（如按钮连续点击、技能重复触发请求），必须设立输入防抖（Debounce）或状态互斥锁。

---

# 8. 反模式禁令与交付自查清单 (Anti-Patterns & Checklist)

### 8.1 严厉禁止的八大架构反模式

| 序号 | 反模式名称 | 典型错误表现 | 架构危害 | 正确工程落地规范 |
| :---: | :--- | :--- | :--- | :--- |
| **1** | **生命周期越权反调** | 下层组件自启协程并在延时后反向调用 `Manager.Recycle(this)` | 破坏创建者所有权，导致多头管理与竞态 | 下层仅广播事实事件（如 `EntityDiedEvent`）；由管理器统一处理回收 |
| **2** | **脱轨野轮询** | 全局单例直接在引擎原生帧循环中轮询实体的内部状态机 | 绕过局部时钟，慢动作与顿帧下状态机全速运行穿帮 | 状态机由实体私有时钟在受控 Tick 中自治推进 |
| **3** | **全局强引用泄漏** | 单例服务在静态全局字典中缓存实体的私有运行上下文 | 实体销毁后因单例强引用导致整棵对象树无法释放 | 上下文由实体自持有，随实体销毁自动 Dispose，单例仅做无状态查询 |
| **4** | **迪米特法则穿透** | 下层组件绕过所属上下文直接跨层访问顶层管理器的活跃对象 | 产生隐藏耦合，小队切人或目标改变时引发空指针或脏读 | 严格基于自身注入的上下文操作，依赖显式注入 |
| **5** | **双向循环依赖** | 模块 A 代理模块 B，模块 B 内部又直接强引用模块 A | 逻辑死锁、初始化时序混乱、难以进行单元测试 | 提取单向抽象契约或引入事件中介彻底解耦 |
| **6** | **全局野时间滥用** | 在业务逻辑、位移计算中随意裸写全局引擎的原始时间步长 | 导致慢动作下位移脱节、技能判定漂移、太空步穿帮 | 严格使用自顶向下下发的受控步长或实体局部时钟步长 |
| **7** | **表现反向篡改逻辑** | 动画关键帧或特效播放完成回调直接修改实体数值或清除状态 | 逻辑结果与帧率、动效绑定，无法实现确定性重放 | 逻辑权威！状态由领域倒计时决定，表现层只读消费 |
| **8** | **静态资产当状态槽** | 在只读配置数据资产（如配置表、配置文件）中记录运行时动态变量 | 多个实体数据交叉污染；编辑器模式下数据意外覆写硬盘 | 配置资产严格只读，易变状态收敛在实体私有状态容器中 |

---

### 8.2 交付自查核对表 (Engineering Review Checklist)

在任何功能开发、系统重构或代码提交（PR / Commit）前，必须完成以下自查：

- [ ] **分层依赖检查**：命名空间与目录物理路径是否严格对齐？是否存在越级逆向依赖（底违引上、玩法直接引用表现具体类）？
- [ ] **数据正交隔离**：配置资产是否纯只读？运行时易变状态是否与实体私有容器隔离？表现层是否纯粹作为单向消费者？
- [ ] **装配机制自愈**：表现层是否通过依赖倒置或自注册工厂接入实体？当预制体资产缺少特定表现组件时，是否存在安全兜底与自愈装配？
- [ ] **时间体系合规**：玩家输入窗口是否使用的是无缩放物理时间？涉及实体位移与判定的代码是否均使用了受实体局部时钟缩放的步长？
- [ ] **生命周期闭环**：对象由谁创建就由谁负责销毁？所有事件监听是否在销毁/回收时进行了反注册？是否存在未取消的游离异步任务？
- [ ] **API 封装收敛**：访问修饰符是否默认收敛（`private` 优先）？对外暴露的集合数据是否使用了只读契约（`IReadOnlyList` 等）？
- [ ] **编译与静态分析**：是否完成全量编译，达到 `0 Errors` 标准？
